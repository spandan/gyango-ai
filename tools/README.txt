Repo-local dev helpers (not shipped in the app).

android/   Shell helpers for Play Asset Delivery and Android build ergonomics.
python/    Offline utilities (e.g. text cleanup parity with Kotlin formatters).

Gradle modules stay addressable by logical name (e.g. :feature-chatbot) via settings.gradle.kts projectDir.
