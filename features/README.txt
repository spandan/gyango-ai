Product feature modules (Android libraries). Each subfolder is a Gradle project; logical name is set in settings.gradle.kts (e.g. :feature-chatbot → features/chatbot).

chatbot/   Compose chat UI, voice, document extractors.
