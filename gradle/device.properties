# Applied when gyango.buildTarget=device (default) or GYANGO_BUILD_TARGET=device.
# Use for Play internal testing, physical devices, and CI — same knobs as production.
#
# Precedence: a key set with ./gradlew -Pkey=value is not overwritten by this file.

# LiteRT: do not enable AVD-only native mitigations (see gyango-litert-inference).
gyango.emulatorLitertWorkarounds=false

# Add more device/Play-only Gradle properties below as you need them, and mirror in emulator.properties
# when the emulator build should differ.
