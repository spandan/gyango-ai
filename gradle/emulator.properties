# Applied when gyango.buildTarget=emulator or GYANGO_BUILD_TARGET=emulator.
# Use for local AVD work; ./tools/android/run-pad-emulator.sh passes -Pgyango.buildTarget=emulator by default.
#
# Precedence: a key set with ./gradlew -Pkey=value is not overwritten by this file.

# LiteRT: WebGPU skip, XNNPack disk cache omit, external cache workspace, etc.
gyango.emulatorLitertWorkarounds=true

# Add more emulator-only Gradle properties below; keep gradle/device.properties in sync for promotion.
